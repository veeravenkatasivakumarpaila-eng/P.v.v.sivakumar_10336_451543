//6. Write an SQL query to create a table Departments with DepartmentID (PK) and
DepartmentName.

CREATE TABLE Departments (
    DepartmentID   INT PRIMARY KEY,
    DepartmentName VARCHAR(100) NOT NULL
);
