1.Get all employees who belong to department 10.
SELECT * FROM employees
WHERE department_id = 10;

2. Display the unique job titles in the employee table.
SELECT DISTINCT job_title
FROM employees;

3. Count how many employees are in each department.
SELECT department_id, COUNT(*) AS total_employees
FROM employees
GROUP BY department_id;

4. Find the highest salary in the employees table.
SELECT MAX(salary) AS highest_salary
FROM employees;

5. Show the names of employees earning more than 50,000.
SELECT name
FROM employees
WHERE salary &gt; 50000;

6. List departments along with their average salary.
SELECT department_id, AVG(salary) AS avg_salary
FROM employees
GROUP BY department_id;

7. Show employees whose names start with &#39;A&#39;.
SELECT * FROM employees
WHERE name LIKE &#39;A%&#39;;

8. Count total employees.
SELECT COUNT(*) AS total_employees
FROM employees;

9. Get the minimum salary for each job role.
SELECT job_title, MIN(salary) AS lowest_salary
FROM employees
GROUP BY job_title;

10. List employees sorted by salary in descending order.
SELECT * FROM employees
ORDER BY salary DESC;

10 Medium SQL Questions and Answers

1. Find departments where total salary is more than
100,000.
SELECT department_id, SUM(salary) AS total_salary
FROM employees
GROUP BY department_id
HAVING SUM(salary) &gt; 100000;

2. Display employees along with their department names
(JOIN).
SELECT e.name, d.department_name
FROM employees e
JOIN departments d
ON e.department_id = d.department_id;

3. Find employees who earn more than the average salary
in their department (correlated subquery).
SELECT e1.*
FROM employees e1
WHERE e1.salary &gt;
(SELECT AVG(e2.salary)
FROM employees e2
WHERE e2.department_id = e1.department_id);

4. Show departments that have more than 5 employees.

SELECT department_id, COUNT(*) AS total_employees
FROM employees
GROUP BY department_id
HAVING COUNT(*) &gt; 5;

5. Display each customer&#39;s total number of orders.
SELECT customer_id, COUNT(*) AS total_orders
FROM orders
GROUP BY customer_id;

6. Show customers who placed more than 3 orders.
SELECT customer_id, COUNT(*) AS total_orders
FROM orders
GROUP BY customer_id
HAVING COUNT(*) &gt; 3;

7. List all employees and the projects they work on
(JOIN).
SELECT e.name, p.project_name
FROM employees e
JOIN employee_projects ep
ON e.employee_id = ep.employee_id
JOIN projects p
ON ep.project_id = p.project_id;

8. Find employees hired after the average hire date.
SELECT *
FROM employees
WHERE hire_date &gt;
(SELECT AVG(hire_date) FROM employees);

9. Show the highest salary in each department.
SELECT department_id, MAX(salary) AS highest_salary
FROM employees
GROUP BY department_id;

10. Find employees who have never placed an order
(LEFT JOIN).

SELECT c.customer_id, c.customer_name
FROM customers c
LEFT JOIN orders o
ON c.customer_id = o.customer_id
WHERE o.order_id IS NULL;

Find employees who earn more than the average salary of their own department.
Query:
SELECT e1.employee_id, e1.name, e1.salary, e1.department_id
FROM employees e1
WHERE e1.salary &gt;
(SELECT AVG(e2.salary)
FROM employees e2
WHERE e2.department_id = e1.department_id);

Find employees who earn the highest salary
in their department
SELECT e1.*
FROM employees e1
WHERE e1.salary =
(SELECT MAX(e2.salary)
FROM employees e2
WHERE e2.department_id = e1.department_id);

2. Find products priced above the average price of their category
SELECT p1.*
FROM products p1
WHERE p1.price &gt;
(SELECT AVG(p2.price)
FROM products p2
WHERE p2.category_id = p1.category_id);

Find students who scored more than the
average score in their class
SELECT s1.*
FROM students s1
WHERE s1.marks &gt;
(SELECT AVG(s2.marks)
FROM students s2
WHERE s2.class_id = s1.class_id);
